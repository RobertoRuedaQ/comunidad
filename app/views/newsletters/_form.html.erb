<div class="row">
  <div class="col-md-6">
   <%= form_with(model: newsletter, local: true, :html => {:multipart => true}) do |form| %>
  <% if newsletter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(newsletter.errors.count, "error") %> prohibited this newsletter from being saved:</h2>

      <ul>
      <% newsletter.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
      <%= form.label :tittle, t('Título') %>
      <%= form.text_field :tittle, id: :newsletter_tittle, class:"form-control" %>
    </div>

        <div class="field">
      <%= form.label :date, t('Fecha del evento') %>
      <%= form.text_field :date, id: :newsletter_tittle, class:"form-control" %>
    </div>


    <div class="field">
      <%= form.label :file, t('Archivo') %>
      <%= form.file_field :file, id: :newsletter_file%>
    </div>

    <div class="actions">
       <%= image_submit_tag("btn-crear.png") %>
    </div>
  <% end %>
 
  </div>
  <div class="col-md-6"></div>
</div>

