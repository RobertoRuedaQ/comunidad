<h2>Iniciar sesión</h2>
<div class="col-md-6 inline-block">
    <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="field">
      <%= f.label :email, t('Correo electrónico') %><br />
      <%= f.email_field :email, autofocus: true,class: "form-control ggg" %>
    </div>

    <div class="field">
      <%= f.label :password, t('Contraseña') %><br />
      <%= f.password_field :password, autocomplete: "off" ,class: "form-control ggg"%>
    </div>

    <% if devise_mapping.rememberable? -%>
      <div class="field">
        <%= f.check_box :remember_me %>
        <%= f.label :remember_me %>
      </div>
    <% end -%>

    <div class="actions">
      <%= f.submit "Iniciar sesión", class: "btn btn-default" %>
    </div>
  <% end %>
</div>
<div class="col-md-6">
  <div class="help-box">
    <%= image_tag('info.png') %>
    <h2>Nota:</h2>
    <p>hola mundo</p>
  </div>
</div>


<%= render "devise/shared/links" %>
