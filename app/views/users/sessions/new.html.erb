<div class="row">
<div class="col-md-6">
  <h2>Iniciar sesión</h2>
    <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="field">
      <%= f.label :email, t('Correo electrónico') %><br />
      <%= f.email_field :email, autofocus: true,class: "form-control ggg" %>
    </div>

    <div class="field">
      <%= f.label :password, t('Contraseña') %><br />
      <%= f.password_field :password, autocomplete: "off" ,class: "form-control ggg"%>
    </div>

    <% if devise_mapping.rememberable? -%>
      <div class="field">
        <%= f.check_box :remember_me %>
        <%= f.label :remember_me, t("Recordarme") %>
      </div>
    <% end -%>

    <div class="actions">
      <%= image_submit_tag("btn-sesion.png") %>
    </div>
  <% end %>
</div>
<div class="col-md-6">
  <div class="help-box">
    <h2><strong>NOTA:</strong></h2>
    <%= image_tag('close.png', class: "close") %>
    <p>Recuerda hacer uso responsable. Hacer de esto un espacio contructivo para todos.</p>
  </div>
</div>


<%= render "devise/shared/links" %>
</div>

