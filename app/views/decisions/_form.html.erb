<%= form_with(model: decision, local: true) do |form| %>
  <% if decision.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(decision.errors.count, "error") %> prohibited this decision from being saved:</h2>

      <ul>
      <% decision.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="col-md-6">
  <div class="row">
    <div class="col-md-3"><%= form.label :tittle, t("Título") %></div>
    <div class="col-md-8"><%= form.text_field :tittle, id: :decision_tittle, class: "form-control ggg" %></div>
  </div>

  <div class="row">
    <div class="col-md-3"><%= form.label :description, t("Descripción") %></div>
    <div class="col-md-8"><%= form.text_area :description, id: :decision_description, class: "form-control ggg" %></div>
  </div>

  <div class="row">
    <div class="col-md-3"></div>
    <div class="col-md-8"><%= form.submit "Crear votación", class: "btn btn-control"%></div>
  </div>
</div>
    
<% end %>
