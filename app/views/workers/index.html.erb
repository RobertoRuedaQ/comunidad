<p id="notice"><%= notice %></p>

<h1>Trabajadores</h1>

<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Apellido</th>
      <th>Arl</th>
      <th>Función</th>
      <th>Memo</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @workers.each do |worker| %>
      <tr>
        <td><%= worker.name %></td>
        <td><%= worker.last_name %></td>
        <td><%= worker.ARL %></td>
        <td><%= worker.role %></td>
        <td><%= worker.memo %></td>
        <td><%= link_to 'Mostrar', worker %></td>
        <td><%= link_to 'Editar', edit_worker_path(worker) %></td>
        <td><%= link_to 'Borrar', worker, method: :delete, data: { confirm: 'Estas seguro?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Crear un nuevo trabajador', new_worker_path %>
